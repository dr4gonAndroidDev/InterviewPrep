package ds.arrays

import java.util.*
import javax.swing.text.html.HTMLDocument

class MapsExamples {

    //DS for storing K-V pairs
    //HASH FUNCTION:
    //given an input , idempotent > gives the same output check based on a hash.

    //SHA 256 takes a lot of time, hence an optimized hash functions are used. other example is MD5 - message Digest 5 ---> O(1)

    fun mapsExamples() {

    }
}

fun main(args: Array<String>) {
    MapsExamples().mapsExamples()
}